declare module '@paypal/checkout-server-sdk' {
    const core: any;
    const orders: any;
    // Add other exports if needed or just use any for flexibility given the time constraint
    export default { core, orders };
}
